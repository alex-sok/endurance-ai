/* Section 04 — the work that does not fit an industry.
   Clients are described by what they run, not by name. */
export function LandingBuilds() {
  return (
    <section className="lp-block" id="builds" data-section="builds" aria-label="Bespoke builds">
      <p className="lp-eyebrow">04 · Bespoke builds</p>
      <h2 className="lp-h2">Not every operation is an industry.</h2>
      <p className="lp-lead">
        Some of what we ship exists once, for one operator, because nothing on
        the market does the job.
      </p>
      <p className="lp-body">
        <b>A private golf club.</b> Member operations, tee sheet and guest
        policy in one system, built to the club&rsquo;s own rules rather than a
        vendor&rsquo;s.
      </p>
      <p className="lp-body">
        <b>A family-law practice.</b> An agreement drafted from an interview,
        assembled clause by clause against state law, returned as a document
        the firm can file.
      </p>
      <p className="lp-body">
        <b>A homebuilder.</b> A configurator that prices a house against the
        parcel it will sit on, so what a buyer designs is what the jurisdiction
        will approve.
      </p>
      <p className="lp-body">
        <b>A consumer marketplace.</b> Catalog, house brands and partner
        inventory under one storefront, launched from nothing.
      </p>
      <p className="lp-body">
        The engagement is the same either way: sit in the operation, find the
        system it was missing, and ship it.
      </p>
    </section>
  );
}
